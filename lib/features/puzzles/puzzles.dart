enum Puzzle {
  dunk('Dunk'),
  timer('Timer'),
  match('Match'),
  basketballPlayer('Basketball Player'),
  conversation('Conversation'),
  training('Training');

  final String name;

  const Puzzle(this.name);
}
